spring:
  application:
    name: basketservice
  data:
    redis:
      host: localhost
      port: 6379
      password: sa

  cache:
    redis:
      time-to-live: 60000
  mongodb:
    uri: mongodb://localhost:27017/basketservice
    database: basketservice

basket:
  client:
    platzi: https://api.escuelajs.co/api/v1
